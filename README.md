# auth-system
Full-stack authentication system featuring secure login, registration, JWT-based authentication, and refresh token flow.

## Environment setup

Use local `.env` files for development and keep only `.env.example` files in Git.

Client:
Copy `client/.env.example` to `client/.env`

Server:
Copy `server/.env.example` to `server/.env`
